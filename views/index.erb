<section class="wrapper">
  <main>
    <div class="container user-profile">
      <div class="holder">
        <h2><%= @user.username %></h2>
        <h3><%= @user.email %></h3>
        <h3><%= @user.age %></h3>
        <% @user.interests.each do |interest| %>
        <div class="group-icon">
          <p><%= interest.interest_name %></p>
        </div>
        <% end %>
        <div class="addition">
          <a href="/">Add more interests</a>
        </div>
      </div>
    </div>
  </main>
</section>
